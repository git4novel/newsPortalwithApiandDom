/* //function declaration
function add(first, second){
    const total = first + second;
    return total;
}
add(21,3);
console.log(add(5,6));


document.getElementById('id').addEventListener('click', function(){

})


// function expression
const add1 = function add1(first, second){

}



function add(first, second){
    const total = first + second;
    return total;
}

function add5(first, second){
    return first + second;
}
const add6 = (first, second) => first + second; //arrow function

// interview qna difference between regular function and arrow function






//down is anonymous function cz to name : function expression with anonymous function
const add2 = function (second, first){

}



 */









const add = (first , second) => first + second;
const  multiply = (a, b) => a * b;


//no parameter arrow function
const getPi = () => 3.1416;

//one parameter arrow func
const fouble = (num) => num * 2;

const divetimes = num => num * 5;


/* //multiline arrow func
const doMath = (x, y, z) => {
    const firstSum = x + y;
    const secondSum = y + z;
    const multiplyResult = 
}

 */
